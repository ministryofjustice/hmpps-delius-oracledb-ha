
---

 # secrets extraction
secrets: "{{ hostvars.localhost.secrets }}"
role_creds_path: "/{{environment_name}}/{{account}}/{{role_name}}/{{inventory_hostname_short}}/"

standby_host: "{{ hostvars[groups[standby][0]]['inventory_hostname'] }}"
primary_host: "{{ hostvars[groups[primary][0]]['inventory_hostname'] }}"
database_standby_unique_name: "{{ hostvars[groups[standby][0]]['database_standby_unique'] }}"
database_global_database_name: "{{ hostvars[groups[primary][0]]['database_global_database'] }}"
database_taf_service_name: "{{ hostvars[groups[primary][0]]['database_taf_service'] }}"
database_primary_unique_name: "{{ hostvars[groups[primary][0]]['database_primary_unique'] }}"
database_primary_sid_name: "{{ hostvars[groups[primary][0]]['database_primary_sid'] }}"

database_password_all: "{{ secrets[role_creds_path + 'database_password_all'] }}"
database_sys_password: "d3l1u5ag41n"

# oracle-database
oracle_database_oracle_home: "/u01/app/oracle/product/11.2.0.4/db"
oracle_grid_oracle_home: "/u01/app/grid/product/11.2.0.4/grid"

# Files
database_primary_orapwfile: "{{ oracle_database_oracle_home }}/dbs/orapw{{ database_primary_sid_name }}"

rdbms_service_user:
  name: oracle
  group: oinstall